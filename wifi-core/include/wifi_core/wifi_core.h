#pragma once

namespace vyre::wifi {

/// Returns the library version string.
const char* version() noexcept;

/// Required by Milestone 0: the canonical misery greeting.
const char* hello() noexcept;

} // namespace vyre::wifi
